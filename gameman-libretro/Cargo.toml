[package]
name = "gameman-libretro"
version = "0.1.0"
edition = "2024"

[lib]
name = "gameman_libretro"
crate-type = ["cdylib"]

[dependencies]
gameman = { path = "..", default-features = false }
rust-libretro = { version = "0.3", features = ["unstable-env-commands"] }
bincode = "1"
libc = "0.2"
